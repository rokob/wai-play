# Playing around with WAI

To run:

* cabal install --only-dependencies
* cabal install route-generator
* make Routes.hs
* cabal build
* `cabal run` or `dist/build/wai-play/wai-play`

Then open a browser to http://localhost:3000 or cURL that shit
